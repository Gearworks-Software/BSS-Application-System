{
  "name": "server",
  "version": "0.0.1",
  "type": "module",
  "description": "This is the backend server which will handle communication between the MySQL database server and the two frontends: the desktop application and the Belize Social Security application tracking website.",
  "main": "dist/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "build": "npx tsc",
    "dev": "node --loader ts-node/esm index.ts",
    "nodemon": "npx nodemon --exec node --loader ts-node/esm index.ts"
  },
  "author": "Gearworks Software",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.11",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mysql2": "^3.14.0"
  }
}
